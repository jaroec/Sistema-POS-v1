# Instrucciones para ejecutar SistemaPOS1 (desarrollo)

## Requisitos
- Node.js 18+ / npm
- PostgreSQL (o usar Docker)
- (Opcional) Docker & docker-compose

## Opción A — Usando Docker (recomendado para reproducibilidad)
1. En la raíz del repo (donde está docker-compose.yml) ejecuta:
